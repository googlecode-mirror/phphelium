/* ---------------------------------------------------------------------
*    BOOTSTRAP
*       - these must be set to the location of the core directive
* ---------------------------------------------------------------------- */
core = "{COREDIR}"
site = "{MOBILEDIR}"
precedence = "{SITEDIR}"